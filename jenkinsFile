pipeline {
    agent any 
    stages {
        stage('Running Tests') {
            steps {
                bat 'npx playwright test ./jenkinsTest.spec.ts'
                echo 'Hello world!' 
            }
        }
    }
}