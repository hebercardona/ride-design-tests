pipeline {
    agent any

    environment {
        SUITE_NAME='Playwright'
        CATEGORY='@pricing'
    }

    stages {
        stage('Playwright Install') {
            steps {
                bat '''
                npm install
                npm i -D @playwright/test
                npx playwright install
                '''
            }
        }
        stage('Running Tests') {
            steps {
                bat "npx cross-env ENV=prod npx playwright test -g '${CATEGORY}'"
            }
        }
    }
    post('Publish Report') {
        always {
                publishHTML([
                    allowMissing: false,
                    alwaysLinkToLastBuild: true,
                    keepAll: true,
                    reportDir: 'playwright-report',
                    reportFiles: 'index.html',
                    reportName: 'Playwright Report',
                    reportTitles: ''
])
                echo 'Publishing Report' 
        }
    }
}