pipeline {
    agent any

    environment {
        SUITE_NAME='Playwright'
        CATEGORY='@pricing'
    }

    stages {
        stage('Playwright Install') {
            steps {
                bat '''
                npm i -D @playwright/test
                '''
            }
        }
        stage('Running Tests') {
            steps {
                bat "npx cross-env ENV=prod npx playwright test ./jenkinsTest.spec.ts"
            }
        }
    }
    post('Publish Report') {
        always {
                publishHTML([
                    allowMissing: false,
                    alwaysLinkToLastBuild: true,
                    keepAll: true,
                    reportDir: 'playwright-report',
                    reportFiles: 'index.html',
                    reportName: 'Playwright Report',
                    reportTitles: ''
])
                echo 'Publishing Report' 
        }
    }
}